env: # Environment variables to pass to all tasks
    CIRRUS_CLONE_DEPTH: 1 # No need to clone with the whole history
    CIRRUS_WORKING_DIR: "/tmp/ci" # Where to clone the aosp-builder repo

# store our secrets here
    gitlab_token: "ENCRYPTED[!e835d7f95397d2aef1a78043f6b938afa290f2d5fcb079bc050b4ef77c0566d4960b9188fe374ed77ab50f22b9bd6918!]"

# store information about git here
    device_codename: xyz
    gmail: xyz@gmail.com
    gitlab_username: ImSpiDy
    gitlab_reponame: cirrus-cache

task:
    name: aosp
    timeout_in: 260m
    container:
      image: aarunalr/aosp-build
      cpu: 8
      memory: 32G
    download_ccache_background_script: # To download ccache in background
      - bash download_ccache # Ccache download script
    sync_script:
      - bash sync
    build_script:
      - bash build
    upload_ccache_script: # Upload ccache to cloud foreground
      - bash upload_ccache # Ccache uploading script
